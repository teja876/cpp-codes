#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    int n;
    string s;
    while (t--)
    {
        cin >> n;
        vector<string> name(n+1);
        vector<int> ranks(n+1, 0);
        for(int i = 0; i < n; i++){
            cin >> s;
            ranks[i+1] = s.end();
            name[i] = ;
        }
        
    }
    
}